<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.team7.godai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PubActivity"/>
        <activity android:name=".activities.ExpActivity"/>
        <activity android:name=".activities.RegisterActivity"/>
        <activity android:name=".activities.User_edit"/>
        <activity android:name=".activities.Courier_edit_P"/>
        <activity android:name=".activities.User_edit_P"/>
        <activity android:name=".activities.Location_edit_P"/>
        <activity android:name=".activities.Update_user_P"/>
        <activity android:name=".activities.Update_user"/>
    </application>

</manifest>
